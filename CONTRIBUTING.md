# 参与贡献

感谢你愿意改进真汇算。

## 提交问题

请先搜索现有 Issue。新 Issue 尽量说明：

- 使用的浏览器与版本；
- 真汇算版本；
- 可复现步骤、预期结果和实际结果；
- 必要时附截图，但请先移除敏感信息。

安全问题请按 [SECURITY.md](SECURITY.md) 私下报告，不要公开披露。

## 本地开发

```powershell
npm install
npm test
npm run serve
```

访问 `http://127.0.0.1:8765/popup.html` 预览界面。也可以在浏览器扩展页加载本项目根目录。

## Pull Request

1. 从 `main` 创建分支。
2. 保持修改聚焦，并为逻辑变更补充测试。
3. 运行 `npm test`。
4. 不要加入远程脚本、分析统计、广告或与单一功能无关的权限。
5. 在 PR 中说明用户可见变化和验证方式。

提交代码即表示你同意按项目的 MIT License 发布贡献。
